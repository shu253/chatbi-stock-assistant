# 安全说明

## 支持范围

当前项目是本地作品集与教学演示应用，默认只建议监听本机地址。未经鉴权、限流和反向代理配置，不应直接暴露在公网。

## 已采用的保护

- DashScope、Tushare 和 Tavily 密钥只从环境变量读取；
- 模型生成的 SQL 只允许单条 `SELECT`/`WITH ... SELECT` 查询；
- SQLite 以只读、`query_only` 模式连接；
- SQLite authorizer 限制为白名单表，并拒绝写入、DDL、ATTACH 和 PRAGMA；
- 查询设置最大 SQL 长度、最大返回行数与执行超时；
- 运行图片保存在 Git 忽略目录并自动清理旧文件。

## 报告问题

请不要在公开 Issue 中提交 API Key、数据库文件或包含隐私的数据样本。发现安全问题时，请通过仓库维护者提供的私密联系方式报告。

